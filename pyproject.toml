[project]
name = "devpilot"
version = "0.1.0"
description = "DevPilot — developer voice copilot built on Rasa Skills (Mantle) + Deepgram"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "rasa-pro==3.20.0.dev6",
    "python-dateutil>=2.9.0",
    "python-dotenv>=1.0.0",
    "slack-sdk>=3.44.1",
]

[tool.uv]
prerelease = "allow"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lib"]
