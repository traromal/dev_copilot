schema:
  public:
    reminder_text:
      type: text
      description: What the developer wants to be reminded about.
      llm_settable: true
    due_minutes:
      type: number
      description: Minutes from now when the reminder is due.
      llm_settable: true
